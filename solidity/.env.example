# RPC endpoint used for deployments / verification (optional; tests use the
# in-process network and need none of these).
RPC_URL=

# Deployer private key (never commit a real key).
PRIVATE_KEY=

# Block explorer API key for contract verification.
ETHERSCAN_API_KEY=
